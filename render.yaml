services:
  - type: web
    name: Step4-1pos-back
    env: python
    plan: free
    branch: main
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
    pythonVersion: 3.12.3
